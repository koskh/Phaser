import { ETileType } from '../../config';

export default function findMatches(
  tile: IPositionInTile,
  type: ETileType,
  grid: ETileType[][],
): IPositionInTile[] {
  console.log('tile', tile);
  console.log('type', type);
  console.log('grid', grid);

  return [];
}
